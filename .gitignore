# AI assistant / agent artifacts (never publish to npm or share with consumers)
CLAUDE.md
.claude/
.omc/
.cursor/
.aider*
.codex/

# Analyzer build artifacts (DLL is rebuilt by CI before publish)
Analyzer~/bin/
Analyzer~/obj/
Analyzers/*.dll
Analyzers/*.pdb
Analyzers/*.deps.json

# OS junk
.DS_Store
Thumbs.db
